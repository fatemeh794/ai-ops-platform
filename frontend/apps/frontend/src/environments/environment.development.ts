// Used automatically by `nx serve` / the `development` build configuration
// (see apps/frontend/project.json -> targets.build.configurations.development).
export const environment = {
  production: false,
  apiUrl: 'http://localhost:8000/api',
};
