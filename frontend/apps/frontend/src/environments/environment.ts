// Production build config. Swap `apiUrl` for your deployed backend's URL
// once this app is hosted somewhere other than localhost.
export const environment = {
  production: true,
  apiUrl: 'http://localhost:8000/api',
};
